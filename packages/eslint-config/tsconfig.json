{
	"extends": ["../../tsconfig.base.json", "@zayne-labs/tsconfig/bundler/no-dom/library"],

	"compilerOptions": {
		"paths": {
			"@/*": ["./src/*"]
		}
	},

	"include": ["**/*.ts"],
	"exclude": ["dist", "node_modules"]
}
