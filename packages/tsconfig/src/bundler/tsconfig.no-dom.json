{
	"extends": ["@zayne-labs/tsconfig/base"],

	"compilerOptions": {
		/* Bundler Mode: */
		"module": "Preserve",
		"target": "ESNext",
		"noEmit": true,
		"jsx": "preserve",
		"allowImportingTsExtensions": true,

		/* If your code doesn't run in the DOM: */
		"lib": ["ESNext"]
	}
}
